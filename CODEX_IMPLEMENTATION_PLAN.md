# План Для Codex: Новый Личный Сайт (Фотогалерея + Блог)

## Правило Работы С Чекбоксами
- Агент Codex обязан менять `- [ ]` на `- [x]` по мере выполнения.
- В каждом этапе сначала закрываются чекбоксы `Выполнение`, потом `Валидация`.
- Чекбоксы `Валидация` можно закрывать только после фактической проверки командами/результатами.
- Если этап завершен, агент отмечает итоговый пункт `Этап завершен`.

## Этап 1. Подготовка И Фиксация Требований
Цель: согласовать структуру нового сайта и правила контента.

Чекбоксы выполнения:
- [x] Зафиксировать целевую структуру: `Home`, `Gallery`, `Photo`, `Blog`, `Post`, `About`, `404`.
- [x] Зафиксировать приоритет: фото как основной контент, текст как вторичный.
- [x] Зафиксировать визуальное направление (типографика, палитра, плотность сетки, mobile-first).

Чекбоксы валидации:
- [x] Создан документ требований (например, `docs/requirements.md`).
- [x] В документе есть явные критерии приемки для галереи и блога.
- [x] Все спорные решения помечены как `TODO/Decision`.

Итог этапа:
- [x] Этап завершен.

## Этап 2. Выбор И Инициализация Стека
Цель: развернуть новую основу проекта под статическую генерацию.

Чекбоксы выполнения:
- [x] Выбрать стек: `Astro + Markdown/MDX` (рекомендуется) или явно зафиксировать альтернативу.
- [x] Инициализировать проект в текущем репозитории (без потери git-истории).
- [x] Добавить базовые npm-скрипты: `dev`, `build`, `preview`, `lint` (если есть).

Чекбоксы валидации:
- [x] `npm install` выполняется без ошибок.
- [x] `npm run build` выполняется без ошибок.
- [x] Локальный предпросмотр открывает стартовую страницу нового сайта.

Итог этапа:
- [x] Этап завершен.

## Этап 3. Модель Контента И Структура Данных
Цель: ввести единый формат для фото и постов.

Чекбоксы выполнения:
- [x] Создать модель контента для `blog` и файловый источник фото из `public/photos/**`.
- [x] Для фото определить источник данных: путь файла как первичный идентификатор, описание опционально через `descriptions.json`.
- [x] Для постов определить поля: `title`, `slug`, `date`, `tags`, `summary`, `draft`, `hero`.
- [x] Зафиксировать структуру директорий для изображений и контента.

Чекбоксы валидации:
- [x] Добавлены минимум 2 тестовых фото-файла и 1 тестовый пост.
- [x] Описание фото действительно опционально и не блокирует сборку при отсутствии.
- [x] Генерация маршрутов по `slug` работает предсказуемо.

Итог этапа:
- [x] Этап завершен.

## Этап 4. Базовые Layout И Компоненты UI
Цель: собрать каркас интерфейса и дизайн-токены.

Чекбоксы выполнения:
- [ ] Создать общий layout с `Header`/`Footer`/контейнером контента.
- [ ] Ввести CSS-токены: цвета, отступы, радиусы, типографика, брейкпоинты.
- [ ] Реализовать базовые компоненты: `Nav`, `Card`, `Tag`, `Pagination`.

Чекбоксы валидации:
- [ ] Главные страницы рендерятся с единым стилем.
- [ ] Верстка корректна на мобильном и десктопе.
- [ ] Нет горизонтального скролла на ширине 320px.

Итог этапа:
- [ ] Этап завершен.

## Этап 5. Галерея И Страница Фото
Цель: реализовать основной пользовательский сценарий просмотра фото.

Чекбоксы выполнения:
- [ ] Реализовать страницу `Gallery` с фильтрацией/группировкой по тегам или сериям.
- [ ] Реализовать страницу `Photo` с крупным изображением и метаданными.
- [ ] Добавить навигацию `предыдущее/следующее` для фото.
- [ ] Включить оптимизацию изображений (`srcset`, lazy-load, современные форматы при возможности).

Чекбоксы валидации:
- [ ] Галерея загружает превью без блокировки первого экрана.
- [ ] На странице фото корректно отображаются метаданные и навигация.
- [ ] Отсутствуют битые ссылки на изображения.

Итог этапа:
- [ ] Этап завершен.

## Этап 6. Блог И Страницы Постов
Цель: сделать легковесный блог для редких текстовых публикаций.

Чекбоксы выполнения:
- [ ] Реализовать страницу списка постов `Blog`.
- [ ] Реализовать страницу поста `Post` с типографикой и поддержкой медиа.
- [ ] Добавить теги и базовую пагинацию (если постов много).

Чекбоксы валидации:
- [ ] Тестовый пост корректно рендерится в списке и по прямой ссылке.
- [ ] Markdown/MDX контент отображается без поломок верстки.
- [ ] `draft`-посты исключаются из production-сборки.

Итог этапа:
- [ ] Этап завершен.

## Этап 7. SEO, RSS, Технические Страницы
Цель: подготовить сайт к индексации и внешним превью.

Чекбоксы выполнения:
- [ ] Добавить мета-теги SEO и Open Graph/Twitter cards.
- [ ] Сгенерировать `sitemap.xml` и `robots.txt`.
- [ ] Добавить `RSS` для блога.
- [ ] Реализовать страницу `404`.

Чекбоксы валидации:
- [ ] Проверить наличие canonical URL на страницах.
- [ ] Проверить корректность OG-превью (title, description, image).
- [ ] `sitemap.xml`, `robots.txt`, `rss.xml` доступны после сборки.

Итог этапа:
- [ ] Этап завершен.

## Этап 8. Производительность И Доступность
Цель: обеспечить быстрый и читаемый сайт.

Чекбоксы выполнения:
- [ ] Оптимизировать размеры изображений и критичные ассеты.
- [ ] Проверить контраст, фокус-состояния, семантику заголовков и alt-тексты.
- [ ] Уменьшить CLS/LCP через правильные размеры медиа и приоритет загрузки.

Чекбоксы валидации:
- [ ] Lighthouse (mobile): Performance >= 90.
- [ ] Lighthouse (mobile): Accessibility >= 90.
- [ ] Lighthouse (mobile): SEO >= 90.

Итог этапа:
- [ ] Этап завершен.

## Этап 9. CI/CD И Деплой На GitHub Pages
Цель: автоматический и повторяемый деплой.

Чекбоксы выполнения:
- [ ] Добавить workflow GitHub Actions: install -> build -> deploy.
- [ ] Настроить деплой в GitHub Pages (ветка/артефакт согласно настройкам репо).
- [ ] Учесть `CNAME` и `base` path (если используется кастомный домен).

Чекбоксы валидации:
- [ ] Workflow проходит на чистом коммите без ручных шагов.
- [ ] Сайт открывается по production-URL после деплоя.
- [ ] Визуально и функционально production соответствует локальной сборке.

Итог этапа:
- [ ] Этап завершен.

## Этап 10. Миграция Контента И Финальный Релиз
Цель: перенести минимум контента и закрыть документацию сопровождения.

Чекбоксы выполнения:
- [ ] Перенести стартовый набор: минимум 3-5 фотосерий и 2-3 поста.
- [ ] Добавить/обновить `README` с инструкциями: локальный запуск, добавление контента, деплой.
- [ ] Подготовить короткий changelog релиза нового сайта.

Чекбоксы валидации:
- [ ] Все стартовые материалы доступны и открываются без ошибок.
- [ ] Команды из README воспроизводимы на чистой среде.
- [ ] Финальная проверка ссылок и маршрутов завершена без критичных ошибок.

Итог этапа:
- [ ] Этап завершен.

## Финальный Чек Готовности Проекта
- [ ] Все этапы 1-10 отмечены как завершенные.
- [ ] Нет блокирующих дефектов по галерее, блогу и деплою.
- [ ] Новый сайт опубликован и подтвержден по публичному URL.
