# План Для Codex: Новый Личный Сайт (Фотогалерея + Блог)

## Правило Работы С Чекбоксами
- Агент Codex обязан менять `- [ ]` на `- [x]` по мере выполнения.
- В каждом этапе сначала закрываются чекбоксы `Выполнение`, потом `Валидация`.
- Чекбоксы `Валидация` можно закрывать только после фактической проверки командами/результатами.
- Если этап завершен, агент отмечает итоговый пункт `Этап завершен`.

## Статус На 2026-02-26
- Последний подтвержденный коммит: `84db098`.
- `Build Site` workflow: `success` (install -> previews -> build).
- `Deploy Pages` workflow: `success` (run `22454496728`).
- Источник Pages переключен на `workflow` (GitHub Actions), конфликт с Jekyll убран.
- Открытая проверка: стабильная доступность кастомного домена `aemelin.ru` (CLI сейчас видит SSL mismatch).

## Этап 1. Подготовка И Фиксация Требований
Цель: согласовать структуру нового сайта и правила контента.

Чекбоксы выполнения:
- [x] Зафиксировать целевую структуру: `Home`, `Gallery`, `Photo`, `Blog`, `Post`, `About`, `404`.
- [x] Зафиксировать приоритет: фото как основной контент, текст как вторичный.
- [x] Зафиксировать визуальное направление (типографика, палитра, плотность сетки, mobile-first).

Чекбоксы валидации:
- [x] Создан документ требований (например, `docs/requirements.md`).
- [x] В документе есть явные критерии приемки для галереи и блога.
- [x] Все спорные решения помечены как `TODO/Decision`.

Итог этапа:
- [x] Этап завершен.

## Этап 2. Выбор И Инициализация Стека
Цель: развернуть новую основу проекта под статическую генерацию.

Чекбоксы выполнения:
- [x] Выбрать стек: `Astro + Markdown/MDX` (рекомендуется) или явно зафиксировать альтернативу.
- [x] Инициализировать проект в текущем репозитории (без потери git-истории).
- [x] Добавить базовые npm-скрипты: `dev`, `build`, `preview`, `lint` (если есть).

Чекбоксы валидации:
- [x] `npm install` выполняется без ошибок.
- [x] `npm run build` выполняется без ошибок.
- [x] Локальный предпросмотр открывает стартовую страницу нового сайта.

Итог этапа:
- [x] Этап завершен.

## Этап 3. Модель Контента И Структура Данных
Цель: ввести единый формат для фото и постов.

Чекбоксы выполнения:
- [x] Создать модель контента для `blog` и файловый источник фото из `public/photos/**`.
- [x] Для фото определить источник данных: путь файла как первичный идентификатор, описание опционально через `descriptions.json`.
- [x] Для постов определить поля: `title`, `date`, `tags`, `summary`, `draft`, `hero` (slug берется из имени файла).
- [x] Зафиксировать структуру директорий для изображений и контента.

Чекбоксы валидации:
- [x] Добавлены минимум 2 тестовых фото-файла и 1 тестовый пост.
- [x] Описание фото действительно опционально и не блокирует сборку при отсутствии.
- [x] Генерация маршрутов по `slug` работает предсказуемо.

Итог этапа:
- [x] Этап завершен.

## Этап 4. Базовые Layout И Компоненты UI
Цель: собрать каркас интерфейса и дизайн-токены.

Чекбоксы выполнения:
- [x] Создать общий layout и контейнер контента.
- [x] Добавить единые `Header`/`Footer` и навигацию верхнего уровня.
- [x] Ввести CSS-токены: цвета, отступы, радиусы, типографика, брейкпоинты.
- [x] Реализовать базовые компоненты: `Nav`, `Card`, `Tag`, `Pagination`.

Чекбоксы валидации:
- [x] Главные страницы рендерятся с единым базовым стилем.
- [ ] Верстка корректна на мобильном и десктопе (после UI-полировки).
- [ ] Нет горизонтального скролла на ширине 320px.

Итог этапа:
- [ ] Этап завершен.

## Этап 5. Галерея И Страница Фото
Цель: реализовать основной пользовательский сценарий просмотра фото.

Чекбоксы выполнения:
- [x] Реализовать страницу `Gallery` с фильтрацией/группировкой по тегам или сериям.
- [x] Реализовать страницу `Photo` с крупным изображением и метаданными (EXIF при наличии).
- [x] Добавить навигацию `предыдущее/следующее` для фото.
- [x] Включить оптимизацию изображений через отдельный шаг превью в CI (ресайз + компрессия, оригиналы доступны для скачивания).

Чекбоксы валидации:
- [x] Галерея загружает превью без блокировки первого экрана.
- [x] На странице фото корректно отображаются метаданные и навигация.
- [x] Фото без описания корректно публикуются и не ломают сборку.
- [x] Отсутствуют битые ссылки на изображения.

Итог этапа:
- [x] Этап завершен.

## Этап 6. Блог И Страницы Постов
Цель: сделать легковесный блог для редких текстовых публикаций.

Чекбоксы выполнения:
- [x] Реализовать отдельную страницу списка постов `Blog`.
- [x] Реализовать страницу поста `Post` с типографикой и поддержкой медиа.
- [x] Добавить теги и базовую пагинацию (если постов много).

Чекбоксы валидации:
- [x] Тестовый пост корректно рендерится в `Blog`-списке и по прямой ссылке.
- [x] Markdown/MDX контент отображается без поломок верстки.
- [x] `draft`-посты исключаются из production-сборки.

Итог этапа:
- [x] Этап завершен.

## Этап 7. SEO, RSS, Технические Страницы
Цель: подготовить сайт к индексации и внешним превью.

Чекбоксы выполнения:
- [x] Добавить мета-теги SEO и Open Graph/Twitter cards.
- [x] Сгенерировать `sitemap.xml` и `robots.txt`.
- [x] Добавить `RSS` для блога.
- [x] Реализовать страницу `404`.

Чекбоксы валидации:
- [x] Проверить наличие canonical URL на страницах.
- [x] Проверить корректность OG-превью (title, description, image).
- [x] `sitemap.xml`, `robots.txt`, `rss.xml` доступны после сборки.

Итог этапа:
- [x] Этап завершен.

## Этап 8. Производительность И Доступность
Цель: обеспечить быстрый и читаемый сайт.

Чекбоксы выполнения:
- [x] Оптимизировать тяжелые изображения через генерацию preview-версий в CI.
- [x] Проверить контраст, фокус-состояния, семантику заголовков и alt-тексты.
- [x] Уменьшить CLS/LCP через правильные размеры медиа и приоритет загрузки.

Чекбоксы валидации:
- [ ] Lighthouse (mobile): Performance >= 90.
- [ ] Lighthouse (mobile): Accessibility >= 90.
- [ ] Lighthouse (mobile): SEO >= 90.

Итог этапа:
- [ ] Этап завершен.

## Этап 9. CI/CD И Деплой На GitHub Pages
Цель: автоматический и повторяемый деплой.

Чекбоксы выполнения:
- [x] Добавить workflow GitHub Actions: install -> generate previews -> build.
- [x] Добавить deploy job в GitHub Actions (artifact/pages deployment).
- [x] Переключить GitHub Pages source на `GitHub Actions` и учесть `CNAME`/`base` path.

Чекбоксы валидации:
- [x] Build-workflow проходит на чистом коммите без ручных шагов.
- [ ] Сайт открывается по production-URL после деплоя.
- [ ] Визуально и функционально production соответствует локальной сборке.

Итог этапа:
- [ ] Этап завершен.

## Этап 10. Миграция Контента И Финальный Релиз
Цель: перенести минимум контента и закрыть документацию сопровождения.

Чекбоксы выполнения:
- [ ] Перенести стартовый набор: минимум 3-5 фотосерий и 2-3 поста.
- [x] Добавить/обновить `README` с инструкциями: локальный запуск, добавление контента, деплой.
- [ ] Подготовить короткий changelog релиза нового сайта.

Чекбоксы валидации:
- [ ] Все стартовые материалы доступны и открываются без ошибок.
- [x] Команды из README воспроизводимы на чистой среде.
- [ ] Финальная проверка ссылок и маршрутов завершена без критичных ошибок.

Итог этапа:
- [ ] Этап завершен.

## Финальный Чек Готовности Проекта
- [ ] Все этапы 1-10 отмечены как завершенные.
- [ ] Нет блокирующих дефектов по галерее, блогу и деплою.
- [ ] Новый сайт опубликован и подтвержден по публичному URL.
